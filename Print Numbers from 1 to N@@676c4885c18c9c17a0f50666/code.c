#include <stdio.h>

int main() {
    int n;
    
    // Input the value of n
    scanf("%d", &n);
    
    // Loop from 1 to n
    for (int i = 1; i <= n; i++) {
        printf("%d ", i);
    }
    
    return 0;
}

