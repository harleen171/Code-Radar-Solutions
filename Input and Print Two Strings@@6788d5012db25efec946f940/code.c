#include <stdio.h>

int main() {
    char str1[100], str2[100];  // Declare arrays to store strings

    // Take two strings as input
    
    scanf("%s", str1);  // Reads a single word (no spaces)
    
    
    scanf("%s", str2);  // Reads a single word (no spaces)

    // Print the two strings
    printf("Second string: %s\n", str1);
    printf("Second string: %s\n", str2);

    return 0;
}
